<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
		http://www.springframework.org/schema/tx 
		http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
		http://www.springframework.org/schema/aop 
		http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
		http://www.springframework.org/schema/task
		http://www.springframework.org/schema/task/spring-task-3.0.xsd">
		
	<bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>classpath:/datasource.properties</value>
				<value>classpath:/dubbo.properties</value>
			</list>
		</property>  
	</bean>

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="maxActive" value="${ds1.maxActive}" />
		<property name="maxIdle" value="${ds1.maxIdle}" />
		<property name="maxWait" value="${ds1.maxWait}" />
		<property name="testWhileIdle" value="${ds1.testWhileIdle}" />
		<property name="timeBetweenEvictionRunsMillis" value="${ds1.timeBetweenEvictionRunsMillis}" />
		<property name="validationQuery" value="${ds1.validationQuery}" />
		<property name="removeAbandoned" value="${ds1.removeAbandoned}" />
		<property name="removeAbandonedTimeout" value="${ds1.removeAbandonedTimeout}" />
		<property name="driverClassName" value="${ds1.driverClassName}" />
		<property name="url" value="${ds1.url}" />
		<property name="username" value="${ds1.username}" />
		<property name="password" value="${ds1.password}" />
	</bean>
	
	<bean id="oraDataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="maxActive" value="${ds2.maxActive}" />
		<property name="maxIdle" value="${ds2.maxIdle}" />
		<property name="maxWait" value="${ds2.maxWait}" />
		<property name="testWhileIdle" value="${ds2.testWhileIdle}" />
		<property name="timeBetweenEvictionRunsMillis" value="${ds2.timeBetweenEvictionRunsMillis}" />
		<property name="validationQuery" value="${ds2.validationQuery}" />
		<property name="removeAbandoned" value="${ds2.removeAbandoned}" />
		<property name="removeAbandonedTimeout" value="${ds2.removeAbandonedTimeout}" />
		<property name="driverClassName" value="${ds2.driverClassName}" />
		<property name="url" value="${ds2.url}" />
		<property name="username" value="${ds2.username}" />
		<property name="password" value="${ds2.password}" />
	</bean>
	
	<!--RedisSession配置 -->
	<bean class="org.springframework.session.data.redis.config.annotation.web.http.RedisHttpSessionConfiguration">
    	<property name="maxInactiveIntervalInSeconds" value="${redis.maxInactiveIntervalInSeconds}"/>
    </bean>
    <!--Redis数据源配置 -->
 	<bean id="jedisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"> 
	    <property name="hostName" value="${redis.hostName}"/>
	    <property name="port" value="${redis.port}"/>
	    <property name="timeout" value="${redis.timeout}"/>
	    <property name="password" value="${redis.password}"/>
	    <property name="database" value="${redis.dbIndex}"/>
	</bean>
	<!--redisTemplate配置 -->
 	<bean id="redisTemplate" class="org.springframework.data.redis.core.RedisTemplate" p:connection-factory-ref="jedisConnectionFactory"/>
	
	<!-- 定时任务 -->
	<!-- 
	<bean id="quarzDemoTask" class="com.huawei.act.task.DemoTask"
		scope="prototype" />
	 -->
	<!-- <task:scheduled-tasks>
		<task:scheduled ref="quarzDemoTask" method="execute"
			cron="0 */30 * * * ?" />
	</task:scheduled-tasks> -->

	<!-- myBatis文件 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<!-- 自动扫描entity目录, 省掉Configuration.xml里的手工配置 -->
		<property name="mapperLocations" value="classpath:com/huawei/act/mapper/**/*.xml" />
		<property name="typeAliasesPackage" value="com.huawei.act.model" />
	</bean>

	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.huawei.act.mapper" />
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
	</bean>
	
	<!-- OramyBatis文件 -->
	<bean id="oraSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="oraDataSource" />
		<!-- 自动扫描entity目录, 省掉Configuration.xml里的手工配置 -->
		<property name="mapperLocations" value="classpath:com/huawei/act/oramapper/**/*.xml" />
		<property name="typeAliasesPackage" value="com.huawei.act.model" />
	</bean>

	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.huawei.act.oramapper" />
		<property name="sqlSessionFactoryBeanName" value="oraSqlSessionFactory" />
	</bean>

	<!-- 配置事务管理器 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- 注解方式配置事务 -->
	<!-- <tx:annotation-driven transaction-manager="transactionManager" /> -->

	<!-- 拦截器方式配置事务 -->
	<tx:advice id="transactionAdvice" transaction-manager="transactionManager">
		<tx:attributes>
			<tx:method name="insert*" propagation="REQUIRED" />
			<tx:method name="update*" propagation="REQUIRED" />
			<tx:method name="delete*" propagation="REQUIRED" />
			<tx:method name="get*" propagation="SUPPORTS" read-only="true" />
			<tx:method name="find*" propagation="SUPPORTS" read-only="true" />
			<tx:method name="select*" propagation="SUPPORTS" read-only="true" />
		</tx:attributes>
	</tx:advice>
	<!-- Spring aop事务管理 -->
	<aop:config>
		<aop:pointcut id="transactionPointcut"
			expression="execution(* com.huawei.act.service..*Impl.*(..))" />
		<aop:advisor pointcut-ref="transactionPointcut"
			advice-ref="transactionAdvice" />
	</aop:config>


</beans>